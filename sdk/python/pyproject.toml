[project]
name = "cortexdb-client"
version = "0.1.0"
description = "Minimal CortexDB HTTP client"
requires-python = ">=3.10"
authors = [{ name = "CortexDB" }]
license = { text = "MIT" }

[tool.setuptools]
py-modules = ["cortexdb_client"]

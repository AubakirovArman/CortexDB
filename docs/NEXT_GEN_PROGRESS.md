# Next-Gen Plan — Progress Ledger

This is the running, honest record of what has actually landed against
[`NEXT_GEN_MASTER_PLAN.md`](NEXT_GEN_MASTER_PLAN.md). It distinguishes a landed
**vertical slice** from the plan's full task scope, so the delta stays visible
rather than being rounded up to "done". Each row links its regression gate.

For the **complete** phase-by-phase status across all tracks (A/B/C/F) — including
which phases are blocked-external or frozen — see
[`NEXT_GEN_PHASE_STATUS.md`](NEXT_GEN_PHASE_STATUS.md).

Status vocabulary:

- **Landed** — merged to `main`, gated, and (where noted) live-verified.
- **Slice** — a usable subset merged; the plan task's remaining scope is listed
  under *Not yet* so it is not mistaken for full completion.
- **Not started** — no code yet.

## Milestone — text-in → context-out loop closed over HTTP

The end-to-end embedding loop works over the HTTP surface and is live-verified
against a real provider (`text_in_context_out_loop_is_closed_live`, ignored by
default):

1. **Write side** — `POST /v1/ingest/text?embed=true` embeds each chunk at
   ingest, or `POST /v1/embedding/backfill` embeds an already-ingested corpus
   idempotently by content hash.
2. **Read side** — `POST /v1/search?embed_query=true` (also `/v1/context`,
   `/v1/search/explain`) embeds a natural-language query at request time, so a
   caller retrieves by meaning without ever supplying a literal `vector=`.

Every step is fail-closed: with no `CORTEXDB_EMBEDDING_*` endpoint configured,
each embedding entry point returns `bad_request` rather than silently degrading.
What remains (see *Not yet* columns) is the contract-grade layer — embedding
profile provenance in the manifest and the CLI-side query embedder.

## Track A — Retrieval quality & embeddings

| Task | State | What landed | Not yet (remaining plan scope) | Gate |
| --- | --- | --- | --- | --- |
| A1.1 Score-fusion scale fix | Landed | `min_max_normalize_q16` + `weighted_retrieval_score` in `retrieval_rank.rs`: all four signals (lexical, semantic, recency, trust) min-max normalized to Q16 before per-mode weighting; no-spread components map to `u16::MAX` to preserve multiplicative decay signals. Cross-mode adversarial tests (Fast lexical, Audit trust, recency tie-break). | **A1.2 corpus-BM25: Slice** — corpus-wide IDF (`CorpusLexicalStats`) is available to the rerank BM25 (raises IDF for pool-common/corpus-rare terms), off by default so goldens are byte-identical; proof test + `retrieval-corpus-bm25-check`. *Remaining:* wire real corpus doc-freq from the persisted index into the engine path behind a knob. A1.3 metric parity (allowlist landed). **A1.4 candidate-pool: Landed** — `--candidate-pool` (default 512, was hardcoded 64) is a first-class knob, emitted as `candidate_pool` in the retrieval report; gate `retrieval-candidate-pool-check`. | `retrieval-recall-baseline-check` |
| A2 (client) HTTPS embedding | Landed | `embed_query_with_config` rewritten on `ureq` (tls) for `http`+`https`; i16 quantization `clamp(-1,1)*i16::MAX`; ignored `live_embed_query_over_https` test (live-verified). | — | server tests |
| A2.1 Embedder adapter | Landed | Engine `Embedder` trait + offline `DeterministicTestEmbedder`. **Provenance (contract-grade):** manifest gains an additive `EMBD` section recording the `EmbeddingProfile` (model/dimension/metric); each embedded cell carries an `embedding_ref=emb1:<model>:<dim>:<metric>:<hash>` payload header (ingest + backfill share one formatter, byte-identical); `Database::open` fails closed on a model/dim/metric mismatch. Additive + backward-compatible: pre-A2.1 DBs open unchanged, and accountability/pack/determinism goldens are untouched (ref lives in payload text, not the descriptor) — verified green. The profile is kept in lockstep with the vector profile (cleared when vectors are gone; stale labels dropped on unconfigured rebuild) and open rejects an internally-inconsistent manifest — both hardened after an adversarial review. | Receipt integration (promote `embedding_ref` into the canonical receipt) is deferred to a C3-5 minor schema bump; the frozen experimental-replication install path does not yet re-stamp the follower profile (documented limitation). | `storage-format-freeze-check` |
| A2.2 Auto-embed at ingest | Slice | Engine `ingest_text_chunks_with_embedder(..)` writes a `vector=` payload header per chunk through an injected `&dyn Embedder` (engine stays network-free). Server `HttpEmbedder` + `embedder_from_env()`; opt-in `POST /v1/ingest/text?embed=true`, parsed fail-closed. **Corpus backfill** `POST /v1/embedding/backfill` drives the engine's pre-existing idempotent, content-hash-keyed `backfill_embedding_debt_batched` over HTTP (batched, `max_items` bound, fail-closed on bad params/no config). Hermetic + ignored live end-to-end tests (live-verified: `embed=true` writes a `vector=` header over HTTPS; backfill embeds an existing cell then converges to zero debt on re-run). | `allow_unembedded=true` job-report escape; checkpoint ACV1/ACH0 interaction; ERB-50 engine-hybrid gate with **no** external vector files; 10k-doc test-embedder → working HNSW; ANN recall gates; crash-between-embed-and-write fail-closed. | `ingest_embedding` tests |
| A2.0 Embedding-model selection | Landed | Selection harness (`scripts/embedding_model_eval.py`, deterministic recall@k/MRR/index-size math + self-test) validates the candidate matrix in `fixtures/embedding/model_selection_v1.json` and asserts the recorded `chosen` profile is the measured winner. **Chosen: `BAAI/bge-m3`, dim 1024, `dot_product`** (67.5 vs 55.7 overall doc recall). Write-up: [`EMBEDDING_MODEL_SELECTION.md`](EMBEDDING_MODEL_SELECTION.md), honest caveat that dense does not fix the `semantic` pool. | Live multi-model eval over ERB-50 + LME-50 (harness supports offline math; broader live candidates deferred). | `embedding-model-selection-check` |
| A2.3 Query-side text→vector | Landed | Server (`/v1/search`, `/v1/context`, `/v1/search/explain`) embeds the query when `embed_query=true`; MCP `search mode=semantic\|hybrid\|auto`; and offline **CLI** `cortexdb search --mode vector\|hybrid\|auto` embeds the query text (no `--vector` needed) via the new shared `cortex-embed-client` crate — the single embedding transport for server **and** CLI, so query and stored vectors are quantized identically and can't drift. Fail-closed everywhere with no endpoint; the CLI reports `vector_source=literal\|embedded\|none`. Live-verified against the real provider. | — | `cli-embedded-query-check` |
| A3.1 Graph-descent HNSW insert | Landed | Standard-HNSW insert replacing the O(N²) full-scan build: deterministic entry point → greedy upper-layer descent → `ef_construction` beam per layer → diversity neighbor-selection heuristic + bounded-degree linking. **Key fix:** `keepPrunedConnections` backfill is applied ONLY to the new node's out-edges (density) and NOT when pruning existing neighbors (which would evict long-range bridges) — with M0 = 2·M on layer 0. This gives **recall 1.0 on the dim-8 synthetic production gate** (`ann-fixture-check`) and 0.95+ on realistic high-dim clustered data, byte-identical double build, bounded degree, and integrity after build+remove. Determinism/pack goldens unchanged (HNSW behind the experimental flag). | — | `hnsw-build-scaling-check` |
| A4.1 Temporal recency window | Slice | Data-relative temporal window in the recency signal (`retrieval_rank.rs`): cells created within `window` seconds of the newest candidate rank as fully fresh; the reference time is the newest created timestamp (no wall-clock, INV-3), so it stays deterministic. Wired via `DatabaseOptions.retrieval_recency_window_seconds`, **off by default** (goldens byte-identical, verified). Recency was already relative/deterministic; this adds the window. | Per-query AQL temporal window + explicit `reference_time` injection (needs the C3-1/C3-5 plan-option protocol). | `retrieval-temporal-window-check` |
| A4.2 Temporal supersession | Slice | Default-off retrieval-time supersession (`retrieval_rank/supersede.rs`) as a `SupersedeOp` after DedupOp: when two retrieved cells state the same temporal fact (`TemporalFactKey` = subject + metric), only the newest is returned, ordered by the maintained temporal index's own `(as_of, created_unix_seconds, cell_id)`. **Reuses the exact `temporal_fact_key` / `temporal_date_for_record` helpers** of the checkpoint-durable `TemporalFactStore` (parity by construction, like A7.1) instead of re-deriving lineage. Conservative — a stale cell is dropped only when a newer sibling is also present in the result set, so a query that surfaces only the older cell still returns it — and wall-clock-free (INV-3). Gated by `DatabaseOptions.retrieval_suppress_superseded`, **off by default** (pack/engine determinism + public-API-freeze verified byte-identical/additive). 6 unit tests + 2 end-to-end `retrieve_aql` tests (off → both fact versions returned; on → only the newest); hardened after an adversarial review that confirmed the default-off no-op and flagged (documented) that — like the existing DedupOp — an *enabled* pass runs before parent-expansion so it can drop a stale fact that is also another cell's parent source. | Global supersession (drop known-stale cells even when the newer is not retrieved, via the maintained `superseded_by`/`latest` index + agent view); per-query AQL surface; explain-visibility of the supersession decision. | `retrieval-temporal-supersede-check` |
| A3.2 Scope-aware traversal | Slice | Codified the sparse-scope exact-fallback threshold as an **explicit named ratio** — `SPARSE_ALLOWED_EXACT_FALLBACK_MAX_RATIO_BPS` (2500 bps = 25%) + `allowed_ratio_within_bps()` — replacing the implicit `available*4 <= graph_nodes`. When the permission-allowed set is ≤ this fraction of the graph, an ANN query exact-scans the allowed set (recall 1.0) instead of spending its HNSW beam on out-of-scope nodes. Behavior-preserving (a test asserts it equals the old check), so ANN goldens are unchanged. | **Ratio now recorded in `AnnSearchReport`** (`sparse_exact_fallback_ratio_bps`, populated on the sparse exact-fallback path) — additive-safe (the report is telemetry, not hashed into the signed pack; public-API-freeze + ann-budget-disclosure verified). Filtered traversal + bounded ef-doubling remain. | `ann-sparse-scope-recall-check` |
| A3.3 Sampled guarded-recall | Slice (core) | Deterministic guarded-recall primitives (`search/ann/guarded_recall.rs`): `should_sample_recall(query_bytes, generation, queries_since_rebuild)` samples the first 32 queries after a rebuild then 1-in-8 by `FNV1a(generation, query) % 8` (no wall-clock/RNG, byte-stable within a generation, reshuffled across rebuilds); `RecallWindow` is a 64-entry ring buffer with `windowed_min` / `is_degraded(floor)` — the signal to fall back to exact serving. Exposed as public API, tested. | Persisting the window per collection in the manifest, wiring the exact-serving switch, and making the degradation state **receipt-visible**. The C3-5 design-review precondition is now **recorded** ([`ADR-ann-degradation-receipt-visibility.md`](ADR-ann-degradation-receipt-visibility.md): the per-collection `ann_serving_epoch` + mode enters the signed surface via an additive-minor bump), so the remaining work is engine implementation + the minor bump/golden re-baseline, not a design gate. | `ann-guarded-sampling-check` |
| A7.1 Field-weighted BM25 parity | Landed | The lexical field-weight table (title 8 / table 6 / path 5 / entity 4 / chunk 2) is now a single frozen source of truth owned by the engine (`lexical_field_weight`, public API), and the EnterpriseRAG-Bench retrieval index **imports it** instead of keeping a private copy — so bench and the governed engine lexical path are parity by construction. Frozen-table test pins the weights (a change must go through C3-1); behavior-preserving (weights already matched). | A7.3 MMR-as-AQL-option (needs the receipt minor-bump). | `lexical-field-weight-parity-check` |
| A7.2 Two-stage retrieve (dense rerank) | Slice | Default-off exact dense **RerankOp** (`retrieval_rank/two_stage.rs`) inserted between RankOp and DiversifyOp in the retrieve pipeline: when `DatabaseOptions.retrieval_two_stage_rerank_weight_q16` is set **and** the query carries a vector, the ranked pool is reordered by `(Q16_ONE−w)·stage1 + w·dense`, where `dense` is the min-max-normalized exact dot (`semantic_dot_score`) against the query vector and `stage1` is the incoming rank position. Integer Q16, no wall-clock/RNG (INV-3); `w=0`/`None` or a vector-less/dimension-mismatched query is a **strict no-op** — pack-determinism, engine-determinism, and public-API-freeze goldens verified byte-identical/additive. Distinct from hybrid RRF (which fuses at candidate-gen): this reranks the **final** pool for *any* stage-1 mode, so even a pure lexical-first retrieval becomes two-stage. 8 unit tests + 2 end-to-end `retrieve_aql` tests (Fast mode: rerank off → lexical title match first; on → the exact dense match promoted to the top). **Measured end-to-end on ERB**: the bench exposes it via `Database::two_stage_rerank_for_task` + `--rerank two-stage`; on the full 511,958-doc corpus (same DB, same lexical shortlist) it lifts recall@10 **54.40% → 60.34% (+5.94)**, `semantic` +7.7 — see [`ERB_ENGINE_PATH_RETRIEVAL.md`](ERB_ENGINE_PATH_RETRIEVAL.md). A depth sweep (`--candidate-limit`) shows the engine's own reranker **reproduces the float reranker's 68.85% to within 1.6 pts**: recall@10 rises **60.34% (pool 64) → 64.33% (256) → 67.29% (1024)**, `semantic` +15.4 (matching the float reranker). A cosine variant (`two_stage_rerank_cosine`) was tested and rejected — byte-identical top-10, so magnitude is not the gap; candidate-pool coverage is. | Last ~1.6 pts is i16-vs-float32 precision; expose the knob as an AQL/plan option with explain visibility (needs C3-1/C3-5 + golden re-baseline). | `retrieval-two-stage-rerank-check` |
| A5 MMR diversification | Slice | Deterministic integer-Q16 Maximal Marginal Relevance reorder (`retrieval_rank/diversify.rs`): first pick = top relevance, then `λ·rel + (1−λ)·(1−max_sim_to_selected)`; no floats / wall-clock (INV-3), ties to lower index. Wired as a `DiversifyOp` after ranking, gated by `DatabaseOptions.retrieval_diversify_lambda_q16` — **off by default**, so all existing goldens are byte-identical (verified). Unit + RetrievedCell end-to-end tests. | AQL surface (`DIVERSIFY λ`) as a plan-visible option (needs the C3-1/C3-5 canonical-set protocol + golden re-baseline); explain contribution of the diversity term. | `retrieval-diversify-check` |
| A6.4 Honest ERB run (exit-proof) | Slice (measured end-to-end, materially improved, not passing) | Honest EnterpriseRAG-Bench retrieval measurement **through the `cortex_engine::Database` product path** (runner `cortex-engine-keyword-source-aware-retrieval`), full **511,958-doc** corpus, balanced-50: lexical **recall@10 = 56.35%** (47 answerable). **Two-stage dense rerank** of the engine's top-50 lexical candidates by BAAI/bge-m3 — the exact pattern A7.2 productizes — lifts this to **recall@10 = 68.85% (+12.5)**, and specifically lifts the flagged pools: `semantic` 23.1→38.5 (+15.4), `basic` 76.5→94.1, `intra-doc reasoning` 75→100. **Now also measured through the engine's OWN reranker** (`--rerank two-stage`, full 511,958-doc corpus, same DB/shortlist): lexical **54.40%**; engine two-stage **60.34%** at candidate pool 64, rising to **67.29%** at pool 1024 — within 1.6 pts of the float reranker's 68.85%, converging as the shortlist deepens. Recorded honestly in [`ERB_ENGINE_PATH_RETRIEVAL.md`](ERB_ENGINE_PATH_RETRIEVAL.md). The raw top-1024 BM25 shortlist already contains **91.25%** of gold docs, so the ≥75% retrieval bar is bounded by the **reranker's top-10 precision**, not stage-1 coverage (both engine i16-dot 67.29% and float-cosine 68.85% leave ~24 pts of in-pool gold below top-10). Answer correctness/completeness is `0.0` (retrieval-only). | Retrieval: a **stronger reranker** (cross-encoder / higher-fidelity than i16 bge-m3 dot) to convert the 91.25% shortlist ceiling into top-10 recall. Answer half: **blocked** — no working LLM chat endpoint (Gemma env var absent from `.env`; DeepSeek key 401); unblock with valid credentials. | `enterprise-rag-bench-cortexdb-retrieval-50` |

| A8.1 Structure-aware chunking | Slice | `split_text_chunks_structured` (`ingestion/chunking.rs`): Markdown-heading segmentation with a heading **breadcrumb**, fenced code blocks and table-row runs kept **atomic** (never split, and `#` inside a code fence is not a heading), and a document-summary **parent** chunk + **child** body chunks carrying `parent_id`+`breadcrumb` (consumed by parent-context expansion + A7.1 field weights). Deterministic (same bytes → same chunk-ids/roles). **Additive**: the paragraph-based `split_text_chunks` is untouched, so ingestion goldens are byte-identical (pack-determinism + public-API-freeze verified). 5 unit tests. | Wire the structured path into the ingest job behind a default-off policy flag + ERB-50 A/B recall proof. | `structure-chunking-check` |

| A8.2 Table fidelity (row-scoped cells) | Slice | `split_table_rows_structured` (`ingestion/chunking.rs`): a table-summary **parent** chunk (column names → a "which columns?" query resolves to the table) plus row-group **child** chunks where each row is tokenized as `header: value` pairs (→ a "column + value" query resolves to the right rows), each prefixed with its source-row provenance (`TableChunkPolicy::cell_range`) and carrying the header breadcrumb + `parent_id`. Configurable rows-per-group; deterministic; additive (golden-safe). 3 unit tests. | Wire into the `/v1/ingest/csv` path behind the structured policy flag + a 200-row query-relevance A/B. | `structure-chunking-check` |

| A5.1 Offline LTR corpus builder | Landed | `scripts/enterprise_rag_bench/build_ltr_corpus.py`: builds a deterministic learned-to-rank corpus — per (question, candidate) the A1.1-normalized Q16 feature vector (lexical/semantic/recency/trust via `normalize_q16`) + a binary gold label — with a **leak-free train/heldout split** on *both* axes (document-connected components ensure no `question_id` **and** no `document_id` crosses the split), stable ordering, downsample ≤50k, and a manifest hash. `--self-test` (60-question synthetic) asserts determinism + no-leak + positives + Q16 range; committed `offline_v2.jsonl` built from the v1 traces. Gate `ltr-corpus-check`. | Train/freeze the Q16 ranker on the corpus (A5.2, via the C3-1 frozen-weights protocol). | `ltr-corpus-check` |

| F1.1 Benchmark-report schema freeze | Landed | Frozen `schemas/benchmark_report.v1.schema.json` (schema_version, `aggregate_stats` with required question counts + recall/correctness/completeness in `[0,100]`, per-type `question_type_stats`, judge model/provider) + a dependency-free validator `scripts/benchmark_report_schema_check.py` that fails if a committed benchmark report is not well-formed. Verified against the committed `erb-submission/official_results.json`. Gate `benchmark-report-schema-check`. | Extend the target list as more benchmark families (LME, LoCoMo) commit results. | `benchmark-report-schema-check` |

| F4.2 AAB-mini six-axis scorer | Landed | `scripts/aab_mini_score.py` + committed `fixtures/aab_mini/systems.v1.json`: the quantitative anti-absorption benchmark on six axes (scope-leak@budget, citation P·R, conflict recall, tokens-to-answer, receipt-verifiability, determinism). The two structural axes are scored **UNRANKED** (not zero) for a system that cannot emit a plan-bound signed receipt / guarantee determinism — the moat's honest argument. Self-test asserts CortexDB is RANKED 6/6 and a thin pgvector+OPA wrapper is UNRANKED on exactly receipt-verifiability + determinism, RANKED on the four imitable axes; deterministic scorer. Gate `aab-mini-score-check`. | Wire a live pgvector+OPA baseline capture (F4.3, needs a docker operator machine) to fill the imitable-axis numbers from a real run. | `aab-mini-score-check` |

| F4.1 Freeze aab_run.v1 schema | Landed | `schemas/aab_run.v1.schema.json` freezes the AAB-mini run contract (per-system per-axis `rank` ∈ {RANKED, UNRANKED}, ranked/unranked counts). The `benchmark-report-schema-check` validator now enforces **both** frozen schemas — `benchmark_report.v1` (ERB official result) and `aab_run.v1` (committed scorer sample `fixtures/aab_mini/aab_run.sample.v1.json`) — so neither an ERB nor an AAB number ships without a schema-valid artifact. | Add the AAB-1 prose spec doc alongside the schema. | `benchmark-report-schema-check` |

| A4.4/A7.3 MMR as an AQL option | Landed | `USING DIVERSITY <lambda_q16>` is now a per-query AQL clause (`parser/retrieve.rs` → `RawRetrieveContext` → `BoundRetrievePlan.diversity_lambda_q16` → the exec `DiversifyOp`, which falls back to the DB default when absent). **Proven golden-safe, not a re-baseline:** the canonical receipt hashes the *resulting* pack (`CONTEXT_PACK_HASHED_FIELDS` = cells/budget/conflicts…), not the query's option flags — so an absent clause yields a byte-identical pack. Verified: pack-determinism, engine-determinism, public-API-freeze all pass; existing AQL parser/binder tests unchanged. Binder test (`USING DIVERSITY 20000` → `Some(20000)`, absent → `None`) + engine e2e test (the clause demotes a near-duplicate through `retrieve_aql`). This reclassifies the AQL-surface "golden-rebaseline" bucket as **golden-safe** (options aren't hashed). | **A7.2 `USING RERANK <weight>` also landed** (same golden-safe pattern → `BoundRetrievePlan.rerank_weight_q16` → the exec RerankOp, DB-default fallback; binder test). **A4.2 `SUPPRESS SUPERSEDED` also landed** (same golden-safe pattern → `BoundRetrievePlan.suppress_superseded` → the exec SupersedeOp; binder + e2e tests). So diversity/rerank/supersede are all plan-visible AQL options now. **A4.1 `RECENCY WINDOW <seconds>` also landed** — all four knobs (diversify/rerank/supersede/recency) are now plan-visible AQL options, every one golden-safe (options aren't in the canonical hash). Remaining: explain-visibility of which options a query applied. | `aql-diversity-option-check` |

| A5.2 Learned-ranker trainer (training half) | Slice | `scripts/enterprise_rag_bench/train_learned_ranker.py`: trains a deterministic Q16 ranker on the A5.1 corpus (Fisher-style per-feature weight ∝ positive/negative mean gap, normalized to sum 65535), and **proves a non-negative held-out MRR lift vs a uniform baseline** — the evidence the C3-1 frozen-weights protocol requires before any ranking weight changes. Deterministic (no RNG/wall-clock). Committed `learned_ranker_v2.json`; `--self-test` asserts Q16 sum, determinism, and held-out non-regression. Gate `learned-ranker-train-check`. *(Weights are coarse on the tiny committed corpus — the value is the deterministic train→freeze→held-out-lift pipeline.)* | Serve the frozen ranker as a **default-off** `DatabaseOptions` profile (golden-safe opt-in) that overrides `plan.weights` when enabled + a larger real-trace corpus. | `learned-ranker-train-check` |

| F5.3 Nightly validation workflow | Landed | Added a `benchmark-validation` job to `.github/workflows/nightly.yml` that runs — nightly + on-dispatch — the 12 benchmark/moat/retrieval/AQL gates landed this cycle (benchmark-report-schema, results-page, aab-mini-score, ltr-corpus, learned-ranker-train, candidate-pool, corpus-bm25, vector-metric-allowlist, two-stage-rerank, temporal-supersede, aql-diversity-option, structure-chunking) and uploads the validation reports as an artifact. YAML validated; every referenced gate exists and passes locally. | Extend as more benchmark families commit committed evidence. | nightly `benchmark-validation` job |
| F1.3 CI lane map + audit | Landed | `fixtures/benchmarks/lanes.v1.json` pins every benchmark/retrieval/moat gate (28 across 3 lanes) to exactly one CI lane with its `required_env`; `scripts/benchmarks/lane_audit.py` asserts each target is a real make target, no gate is double-mapped, and the `benchmark-validation` lane agrees **bidirectionally** with the real `nightly.yml` job (a gate defined-but-unscheduled, or scheduled-but-unmapped, fails CI). `docs/CI_LANES.md` gains the Benchmark-validation lane row + a pointer to the machine map. Gate `benchmark-lane-audit-check`, wired into nightly. | Add the PR + Release lanes to the machine map once their jobs stabilize. | `benchmark-lane-audit-check` |
| F1.2 Committed benchmark registry | Landed | `fixtures/benchmarks/registry/*.json`: one immutable snapshot per benchmark (ERB-500, LongMemEval v1, LoCoMo, MultiHop-RAG) recording metrics + judge + committed-evidence source. `scripts/benchmarks/registry_summarize.py` renders the summary table **and** enforces honesty invariants: ERB's 4 metrics are machine-verified against `erb-submission/official_results.json` (drift fails — proven by a negative test flipping 47.74→99.99), LongMemEval's 5 metrics must literally appear in the cited doc, and **no entry may claim `leaderboard_official` without an official judge** (LoCoMo/MultiHop honestly recorded `no_headline_claimed`). Gate `benchmark-registry-check`, wired into nightly. | Add a snapshot per new official run; extend to per-type breakdowns. | `benchmark-registry-check` |
| F1.4 Fast shared retrieval-eval loop | Landed | `scripts/benchmarks/quick_eval.py` + `fixtures/benchmarks/quick_eval/` (self-contained 12-doc `corpus.jsonl`, 12 typed questions `questions.jsonl` across basic/multi_doc/paraphrase/temporal with `expected_doc_ids`, `degraded_corpus.jsonl`, `registry_baseline_v1.json`). One `make quick-retrieval-eval-check` that **runs real engine retrieval** — it ingests the corpus into a throwaway DB **through the `cortexdb` CLI** and runs one keyword search per question — then scores per-type **recall@10/MRR/nDCG** vs the committed registry baseline, failing on any regression. **Real ranking** (a ranking change moves the numbers, not a cached log); **deterministic** (double-run byte-identical, no timestamps — verified); **offline** (zero LLM/net) and **<1s** (well under the 5-min budget). Degradation is caught two ways: `--self-test` checks the metric math + flags a degraded ranking, and `--degradation-check` runs **real** retrieval over the committed degraded corpus (gold terms stripped) and asserts the regression is caught end-to-end (recall 1.0→0.083, 15 regressions). Becomes the uniform acceptance step for Track A/B ranking tasks (replacing bespoke A/B procedures). Gate in the benchmark-validation lane (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — now 31 gates); documented in `docs/BENCHMARKS.md`. | Add an LME compact-50 slice as a second cached corpus; ranking tasks (A1.x/A4.x/A7.x) cite this gate in their acceptance. | `quick-retrieval-eval-check` |
| F2.0 Judge-of-record decision | Landed | The judge-of-record decision was **stated in the plan** (interim ERB numbers judged by gemini-3.5-flash, comparable to the 47.74 baseline; official/leaderboard header + any `leaderboard_comparable` claim = **gpt-5.4 only**), so this was an **encoding** task, not a user choice (correcting the earlier "needs a user decision" label). Encoded the policy in `docs/PUBLIC_CLAIMS_POLICY.md` (§ Judge of Record) + a new `docs/BENCHMARK_EVIDENCE.md` decision doc with the full evidence trail. The machine rule was already live in the F1.2 summarizer (`leaderboard_official` requires `judge.official`), and the committed ERB registry entry already carries `judge=gemini-3.5-flash / official=false / leaderboard_official=false / interim_non_official_judge / 47.74`. New `scripts/benchmarks/judge_of_record_check.py` (gate `judge-of-record-check`) asserts both docs declare gpt-5.4 official + gemini-3.5-flash interim, and that **no committed registry entry claims leaderboard-official without the judge of record** — self-test proves the official-ok / interim-ok / interim-claiming-leaderboard-rejected cases. Resolves the apparent tension with `RESULTS.md`'s "Gemini judge of record" (that is the *interim in-house* record, correctly non-leaderboard). In the benchmark-validation lane (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — 38 gates). | The metered gpt-5.4 official re-judge run itself is F2.1's key-gated runner; F2.0 fixes the *policy* that run will satisfy. | `judge-of-record-check` |
| F3.4 LoCoMo retrieval regression gate | Landed (retrieval half; QA half key-gated) | `scripts/locomo/retrieval_regression_gate.py` + `fixtures/benchmarks/locomo/{reference,mini_retrieval_log,degraded_retrieval_log}.jsonl` + `retrieval_baseline_v1.json` + `mini_baseline_v1.json`. Implements the LoCoMo IR metrics **hit@1, hit@10** and fails on a drop >0.01 vs the **locked baseline** (hit@1 0.3199 / hit@10 0.6312). Fast offline lane (gate default): unit-checks the hit@k math, asserts the committed baseline values are unchanged, and catches a deliberately-degraded log; `--log/--baseline` gate a real `locomo-cortexdb-retrieval` replay. Same self-tested pattern as F3.1/F3.3. Gate `locomo-retrieval-regression-check` in the benchmark-validation lane + the `validation-nightly` aggregate (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — 37 gates). | The QA end-to-end half (`run_qa.py`/`check_qa_evidence.py`) needs an OpenAI-compatible reader endpoint and stays key-gated — the retrieval regression half is complete and independent. | `locomo-retrieval-regression-check` |
| F2.1 ERB gpt-5.4 re-judge ready/blocked gate | Landed (gate; metered run key-gated) | `scripts/enterprise_rag_bench/check_official_rejudge.py` + `fixtures/benchmarks/erb/official_rejudge_target.v1.json`. The phase's stated deliverable is the **target + ready/blocked gate**, which needs no LLM: it pins the judge of record (**gpt-5.4**) and the `erb-submission/answers.jsonl` **SHA-256**, and **before any judge tokens are spent** verifies the committed answers still match that SHA — a drifted answers file is a hard FAIL (you must not re-judge a changed answer set against the recorded target). It then reports `ready` (the `ENTERPRISE_RAG_BENCH_REJUDGE_API_KEY` env is set) or `blocked` (no key → exit 0, no tokens, explicitly *not* a failure — the gate correctly guarding the metered run). Self-test verifies the ready/blocked/integrity decisions and that the committed answers.jsonl matches the recorded SHA; live run reports BLOCKED cleanly here. Gate `erb-official-rejudge-ready-check` in the benchmark-validation lane (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — 36 gates). | The actual token-spending gpt-5.4 re-judge run (`official_rejudge.py`) runs when the judge key is present; this gate guards it. | `erb-official-rejudge-ready-check` |
| F5.3 Nightly validation program aggregate | Landed | `mk/validation-program.mk` adds the **`validation-nightly`** aggregate that chains the benchmark-validation regression/packaging gates in one sequential run: benchmark-registry → longmemeval-per-type-regression (F3.1) → longmemeval-per-type-report (F2.3) → erb-category-regression → multihop-retrieval-regression (F3.3) → locomo-retrieval-adapter → aab-mini-score → quick-retrieval-eval (F1.4) → benchmark-trend (F5.2). **Runs green end-to-end (verified)** — every gate is deterministic + self-contained (self-tested fast lanes + real dataset adapters), no LLM. The `nightly.yml` benchmark-validation job runs the gates individually (so `benchmark-lane-audit-check` maps each), and its `if: always()` upload now also captures `target/benchmark-registry`, `target/enterprise-rag-bench/category-regression`, `target/locomo`, `target/quick-retrieval-eval`, `target/aab`. **Unblocked this session**: its previously-missing chain links were F3.1/F3.3, both landed above. (Fixed a subtle lane-audit false-positive: the audit's `make <target>` regex matched the literal inside a YAML comment — reworded.) | Add F4.x AAB matrix + future per-type gates to the chain as they land. | `validation-nightly` (+ `benchmark-lane-audit-check`) |
| F2.3 LongMemEval per-type score packaging | Landed | `scripts/longmemeval/per_type_report.py`: packages a LongMemEval run into a per-type `cortexdb.longmemeval.per_type_report.v1` draft snapshot — **per-type QA accuracy** (from the evaluator output) + **recall_all@10 / ndcg_any@10** (from the retrieval log + reference), per the six official question types. Two assertions guard packaging integrity: **all dataset types present** and **recomputed overall QA accuracy == the evaluator's reported overall** (catches an aggregation drift). `--self-test` (gate default) packages a synthetic 12-question run and verifies the per-type means (qa_accuracy/recall each 0.5 by construction), that a missing type fires the all-types assertion, and that a wrong reported-overall fires the match assertion; the `--reference/--retrieval-log/--evaluator-output` path emits a real draft snapshot when an official run's artifacts land. Same self-tested pattern as F3.1/F3.3. Deterministic, stdlib-only, offline. Gate `longmemeval-per-type-report-check` in the benchmark-validation lane (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — 35 gates). | Emit the real per-type snapshot once an official LongMemEval run's retrieval log + evaluator output are committed. | `longmemeval-per-type-report-check` |
| F3.3 MultiHop-RAG retrieval regression gate | Landed | `scripts/multihop_rag/retrieval_regression_gate.py` + `fixtures/benchmarks/multihop_rag/{reference,mini_retrieval_log,degraded_retrieval_log}.jsonl` + `retrieval_baseline_v1.json` + `mini_baseline_v1.json`. Implements the MultiHop IR metrics **Hits@10, Hits@4, MAP@10, MRR@10** and fails on a drop beyond tolerance vs the **locked balanced_50 baseline** (Hits@10 1.0000 / Hits@4 0.9545 / MAP@10 0.4396 / MRR@10 0.7760, the F1.2 v7 snapshot). Fast offline lane (gate default): unit-checks the IR metric math (AP/MRR/Hits), asserts the committed balanced_50 baseline values are unchanged, and catches a deliberately-degraded log end-to-end (4 regressions); the `--log/--baseline` path gates a real balanced_50 replay when the MultiHop repo is available (official-scorer parity deferred to that path). Same self-tested-comparator pattern as F3.1/F2.2/F5.2. Deterministic, stdlib-only, offline. Gate `multihop-retrieval-regression-check` in the benchmark-validation lane (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — 34 gates). | Wire the official `retrieval_evaluate.py` parity check once the MultiHop repo + balanced_50 corpus are present. | `multihop-retrieval-regression-check` |
| F3.1 LongMemEval per-type retrieval regression gate | Landed | `scripts/longmemeval/per_type_regression_gate.py` + `fixtures/benchmarks/longmemeval/{reference,mini_retrieval_log,degraded_retrieval_log}.jsonl` + `per_type_retrieval_baseline_v1.json`. Scores **per-type `recall_all@10` + `ndcg_any@10`** from a retrieval log against the committed baseline, failing if any of the 6 LongMemEval types drops >0.02 (2.0 pts) or overall >0.01 (1.0 pt). The master plan's **fast lane is offline** — the gate default replays the committed 18-row mini-log through the scorer and asserts a deliberately-degraded log is caught end-to-end (14 regressions, recall_all 1.0→0.0); `--log/--baseline` accept a real official log + official baseline when the metered run lands. Same self-tested-comparator pattern as F2.2/F5.2 (logic proven now, real data plugs in later). Also asserts every dataset type is present in the baseline. Deterministic, stdlib-only, offline. Gate `longmemeval-per-type-regression-check` in the benchmark-validation lane (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — 33 gates). | Swap the mini baseline for the official per-type baseline once a LongMemEval official run's retrieval log is committed. | `longmemeval-per-type-regression-check` |
| F5.2 Benchmark-score trend gate | Landed | `scripts/benchmark_trend_check.py`: latest-vs-previous per-benchmark trend comparison against the committed tolerance table (LongMemEval acc −0.01 / recall_all@10 −0.005; ERB combined −1.0 **same-judge only**; MultiHop hits@10 −0.02; LoCoMo hit@10 −0.01), failing on any drop beyond tolerance. **Reuses the F2.2 judge-identity rule**: a judge/reader change on a judge-guarded benchmark (ERB) **refuses** the numeric trend (`comparable:false`, `judge_changed:true`, no delta leaked, not counted as a regression) — a deliberate re-baseline, not a silent pass/fail. `--self-test` proves within-tolerance passes, beyond-tolerance regresses (both LME metrics), improvements never regress, the ERB same-judge drop regresses, and the ERB cross-judge case refuses with no delta; `--previous/--latest/--strict` for real snapshot pairs. Deterministic, stdlib-only, offline. Gate `benchmark-trend-check` in the benchmark-validation lane (`lanes.v1.json` + `nightly.yml`, bidirectionally audited — 32 gates). | The "release-regression dashboard extension" is **N/A** (that dashboard scores release perf metrics — storage/search/verify — not benchmark scores; a benchmark rule doesn't fit its data model). Wire real `--previous/--latest` snapshot pairs once a benchmark has two committed registry snapshots. | `benchmark-trend-check` |
| F2.2 Same-judge guard for ERB comparisons | Landed | `scripts/enterprise_rag_bench/compare_official_runs.py`: given two `official_results.json`-shaped runs, if their `(judge_model, judge_provider)` differ it **refuses** — `refused_cross_judge_comparison: true` with **no delta fields** (`--strict` exits non-zero); only a same-judge pair yields headline + per-question-type deltas. The dynamic complement to F1.2's static `judge.official` guard: F1.2 stops an unofficial number being *published* as comparable; F2.2 stops two runs being *compared* across judges at all. Self-test proves the same-judge delta (+1.50) and that a cross-judge refusal leaks no delta. Gate `erb-compare-runs-check`, wired into nightly. | Feed real gpt-5.4 vs gemini run pairs once an official-judge run exists. | `erb-compare-runs-check` |
| C1-2 (PR lane) Accountability verifiers on every PR | Landed | Wired the **fast, deterministic accountability/receipt verifier subset** into the `rust.yml` PR lane: `accountability-canonical-check` (canonical serialization determinism), `accountability-receipt-tamper-check` (tamper-detection vs a committed fixture), `receipt-threat-model-check`, `gce-spec-doc-check`. These verify committed goldens/docs that `cargo test --workspace` does **not** cover, so a receipt-integrity regression is caught on every PR instead of only nightly. All four verified green + sub-4s locally; YAML validated; `docs/CI_LANES.md` PR row updated. | The heavy subset (`scope-leak-bench-check`, `consensus-release-lane-check`) → nightly needs a verifying run before wiring (not burned here); the "clean-clone CI run" record is produced by the run itself. | PR-lane `rust.yml` |
| F04-B4.4 (MCP tool) `consolidate_plan` + `consolidate_commit` — **B4.4 complete** | Landed | Wired the reference MCP consolidation worker as a two-step tool protocol: `consolidate_plan` (list stale episodic groups) + `consolidate_commit` (record the agent's summary + source byte-refs), in `cortex-mcp` — `ConsolidatePlanArgs`/`ConsolidateCommitArgs` + `ToolExecutor` trait methods + `tools_list` schemas (`tools.rs`), JSON-RPC dispatch + `FakeExecutor` stub + tests (`server.rs`: `lists_six_tools`, `calls_consolidate_tools`), and the `SdkToolExecutor` impl calling the sync SDK `consolidate_plan`/`consolidate_commit` (`sdk_executor.rs`). 11 MCP tests green; fmt/clippy/workspace clean. **This closes F04-B4.4 end-to-end**: engine (B4.2 candidates + B4.3 commit) → HTTP plan/commit routes → Rust SDK methods → MCP tool. | — | `cargo test -p cortex-mcp` |
| F04-B4.4 (HTTP surface) memory-consolidation plan + commit routes | Landed | Wired `POST /v1/memory/consolidate/plan` (read-only, over `semantic_compression_candidates` / B4.2) + `POST /v1/memory/consolidate/commit` (over `commit_semantic_memory_compression` / B4.3) — the two-step surface the reference MCP consolidation worker drives. New `cortex-api-types::memory_consolidation` wire types (plan groups + commit request/report), `cortex-server::memory_consolidation` handlers (agent-auth + `require_read_scope` on plan; agent-id from the view on commit), registered in `router.rs` + `is_agent_scoped_route` + `is_write_route` (commit), OpenAPI-documented (coverage green). Gated on the default-off `semantic_compression` flag. 2 wire round-trip + 4 handler tests (plan returns stale episodic groups, commit records a provenance-preserved auditable summary, auth-required, feature-off inert). fmt/clippy/openapi/codegen-control/workspace clean. **This unblocks B4.4's MCP tool** (the endpoints it needs now exist). Gate `memory-consolidate-route-check`. | B4.4 remaining: SDK methods + the reference MCP `consolidate_memory` tool over these endpoints. | `memory-consolidate-route-check` |
| F04-B6.3 (TypeScript SDK) `agentTransaction` + `agentHandoff` — **B6.3 complete** | Landed | Added `agentTransaction(request)` + `agentHandoff(request)` to `sdk/typescript/cortexdb-client/client.ts` (POST the JSON body to `/v1/transactions` + `/v1/handoff`, return `JsonObject`, mirroring `tombstoneCell`/`flush`; `.d.ts` re-exports `CortexDBClient` from source so the method surfaces automatically). Rebuilt the committed ESM + CJS bundles (clean +24-line diff, no spurious rebundle) and extended `type-smoke.ts`; `npm run typecheck` + `npm test` (10 tests) green under node 24; OpenAPI-SDK codegen-control + `check_sdk_contract` gates green. **This closes F04-B6.3 end-to-end**: engine (B1.3) → wire contract → both live server routes → all SDK clients (Rust sync + async, Python, TypeScript). | — | `cd sdk/typescript && npm run typecheck && npm test` |
| F04-B6.3 (Python SDK) `agent_transaction` + `agent_handoff` methods | Landed | Added `CortexDBClient.agent_transaction(request)` + `agent_handoff(request)` to the pure-Python SDK (`sdk/python/_cortexdb_client/client.py`), POSTing the JSON request to `/v1/transactions` + `/v1/handoff` and returning the parsed dict (mirroring `search`/`flush`; tenant threaded by `_request`). Docs note the conflict semantics (read `outcome`, not the status code). Capture-opener test asserts both methods POST to the right paths with the request body; full `test_cortexdb_client.py` suite (15 tests) green; OpenAPI-SDK codegen-control + generated-types gates still green (no migrated-component change). | TypeScript SDK method remains (needs the esbuild toolchain). | `python3 -m pytest sdk/python/test_cortexdb_client.py` |
| F04-B6.3 (Rust SDK) `agent_transaction` + `agent_handoff` methods | Landed | Added the client methods to **both** Rust clients — `CortexDbClient` (sync) and `AsyncCortexDbClient` (async) — POSTing the frozen `AgentTransactionRequestBody`/`AgentHandoffRequestBody` to `/v1/transactions` + `/v1/handoff` and decoding the typed responses, mirroring the existing `context`/`aql` method pattern. Re-exported the wire types through the SDK's `types` module (from `cortex_api_types::agent_transaction`) + crate root, incl. `transaction_outcome_status`. The docs note the conflict semantics: **read `outcome`, don't rely on the status code** (a conflict is a 200 body). Wire-stability test (tagged `op` serialization, conflict-response decode, path stability) passes at the same unit level as every other SDK method; full `cortexdb-sdk` suite (37 tests) green; fmt/clippy/workspace clean. | Python + TS SDK methods remain (separate toolchains, spawned task_fe4b21d6). | `cargo test -p cortexdb-sdk` |
| F04-B6.3 (transactions route) `POST /v1/transactions` live | Landed | Wired the live HTTP route over `commit_agent_transaction` (idempotency ledger F04-B1.3): `handle_transactions_shared` parses `AgentTransactionRequestBody`, requires an authenticated agent + `require_write_scope`, builds a `WriteBatch` from the tagged `WriteOpRequest` list, commits as the caller's agent, and returns `AgentTransactionResponse`. **Resolved the conflict-status blocker additively** (no shared-pipeline change): a conflict is a normal **`200` with `outcome:"conflict"`** + the full `conflicts[]` — the request *was* processed, the outcome is domain data, so the body carries the detail rather than an error envelope (the frozen `transaction_outcome_status` helper is refined to the *logical* status an SDK surfaces). Route registered (`router.rs` + `is_agent_scoped_route` + `is_write_route`), OpenAPI-documented (coverage green). 4 tests: commit + **idempotent replay** (`idempotent_replay:true`), **conflict-as-200-body** (stale base_seq → `stale_cell` detail), auth-required, feature-off inert. fmt/clippy/openapi/workspace clean. Gate `agent-handoff-route-check` (now both routes). | The 3 SDK client methods remain (spawned task_21e0d830). | `agent-handoff-route-check` |
| F04-B6.3 (handoff route) `POST /v1/handoff` live | Landed | Wired the live HTTP route over the F08-B6.1 handoff-ledger: `crates/cortex-server/src/agent_transaction.rs::handle_handoff_shared` parses `AgentHandoffRequestBody`, requires the caller be authenticated **as the source agent**, resolves the **target view via `db.load_agent_view`**, calls `commit_agent_handoff`, and returns `AgentHandoffResponse`. Registered in `router.rs` (+ `is_agent_scoped_route` + `is_write_route`), documented in `docs/openapi.yaml` (coverage gate green). Commit-or-error, so status maps cleanly through the existing `RouterError` taxonomy — **no taxonomy change needed** (that's only the transactions route). 4 tests (commit+record, source-auth-mismatch/anon rejected, unknown-target→400, feature-off inert) — provisioning the target via `save_agent_view`, which dissolved the earlier "needs fresh context" blocker. fmt/clippy/openapi/workspace-check clean. Gate `agent-handoff-route-check`. | The transactions route needs the `Conflict`/409 taxonomy extension; both feed the 3 SDKs (spawned task_21e0d830). | `agent-handoff-route-check` |
| F04-B6.3 (contract) Agent-transaction/handoff HTTP wire types | Landed | Froze the neutral v1 wire contract for `POST /v1/transactions` + `POST /v1/handoff` in `cortex-api-types` (`agent_transaction.rs`): `AgentTransactionRequestBody`/`WriteOpRequest`(tagged put/patch/tombstone)/`AgentTransactionResponse`/`…ConflictResponse` and `AgentHandoffRequestBody`/`AgentHandoffResponse`, plus `transaction_outcome_status` (committed→200, conflict→409). Mirrors the engine types 1:1 so the live route + all three SDKs share one contract (like the F1.1/F4.1 schema freezes). 4 serde round-trip + mapping tests; clippy clean. Gate `agent-transactions-contract-check`. The live route wiring (auth→AgentView + target-view resolution + WriteBatch deserialize) is the remainder, spawned as a focused task. | Wire the routes + SDK methods against this frozen contract. | `agent-transactions-contract-check` |
| C4-2 (JCS + Merkle + Ed25519) Cross-language crypto-primitive parity | Landed | Proved **all three core cryptographic primitives** of the accountability receipt reproduce cross-language byte-for-byte (Rust↔Python), so the receipt spec is normative, not Rust-specific: **(1) JCS canonical JSON** (8 vectors, sha256 byte-match); **(2) blake3 Merkle roots** (`receipt.rs::merkle_root`, 5 vectors 0–4 leaves — leaf/node domains, odd-node duplication, empty-tree); **(3) Ed25519 signing** (`ReceiptSigningKey`, 3 vectors — deterministic RFC 8032 over the domain-wrapped `cortexdb.accountability_receipt.sign.v1‖0x00‖msg`, matching Python `cryptography` signatures + public keys exactly). Python (`scripts/canonical_jcs_cross_language_check.py`) re-derives each; Rust tests (`canonical::tests::jcs_…`, `accountability::receipt_tests::merkle_…`/`ed25519_…`) assert Rust == the committed Python-derived fixtures. **Verify-only, golden-safe**; Rust holds the proof even where blake3/cryptography aren't pip-installed (Python re-checks skip gracefully). Gate `canonical-jcs-cross-language-check`, in nightly + F1.3 lane map. **Plus a real-receipt check:** Python re-derives the **6 Merkle roots of the actual committed receipt** (`fixtures/accountability_receipt/verify_input.golden.json`) from its committed leaves — matching the Rust-produced header roots byte-for-byte. This surfaced (and now replicates) the receipt's `sorted_values` step: roots are over leaves **sorted by canonical bytes** (the golden stores them unsorted; the 2-leaf `budget` family exposed it). So the receipt-body Merkle composition is verified cross-language on real data, not just synthetics. | **`pack_root` now verified cross-language too (data-boundary dissolved):** rather than re-derive from a golden that lacks cell payloads, we *generate* a fresh minimal-pack vector — a one-cell `ContextPack` whose plain payload carries no `source_id=`/`source=`/`citation=` header (so `metadata.source_ref` is `None`) with every optional (citation, source_ref, provenance, explain, access_decision, grounding_report) and anomaly absent — making `canonical.rs::context_pack_value` a total, faithfully-reproducible mapping. Python (`context_pack_value` mirror) computes the committed `canonical_pack_bytes_hex` + `pack_root`; Rust (`accountability::receipt_tests::pack_root_matches_cross_language_vector`) builds the same pack through the **real engine** `canonical_context_pack_bytes` and asserts identical bytes + root (`fixtures/canonical/pack_root_conformance_vector.v1.json`). So the pack canonicalization *mapping* is proven language-independent, not just the JCS/Merkle/Ed25519 primitives. The earlier "not possible from committed goldens" finding stands (the *response*-form pack omits payloads) — this sidesteps it with a purpose-built golden. **Plus 3 of the 6 leaf-family *extractions* now verified cross-language:** `budget_leaves` + `conflict_leaves` (pure pack-scalar functions, no cell-content hash) via the minimal pack, and `access_leaves` (self-contained: each admitted leaf's `evidence_digest` is a blake3 `hash_value` over a small access-evidence object; denied leaves carry a precomputed digest) via the deterministic `sample_receipt_inputs` decision + denial. The Python mirrors re-derive the exact leaf Values (incl. the access `evidence_digest`); Rust (`pack_leaf_families_match_cross_language_vector` + `access_leaves_match_cross_language_vector`) asserts the real engine `receipt_leaves::{budget,conflict,access}_leaves` produce identical Values — closing the *inputs→leaves* half for those families (the Merkle *roots* over leaves were already verified). **Plus `cell_set` + `provenance` (→ 5 of 6 leaf families):** these carry `cell_content_hash = blake3(CELL_HASH_DOMAIN ‖ canonical_cell_bytes)`, where `canonical_cell_bytes` wraps cell_id + payload_hex + `descriptor_value(descriptor)` (23-field 1:1 copy). Rather than reproduce the `CellDescriptor` *parser* (upstream of the receipt), we pin an **explicit** descriptor — every field known in both languages — so the Python mirror hashes the same fields and Rust (`cell_content_leaf_families_match_cross_language_vector`) runs the real engine `cell_content_hash` + `cell_set_leaves`/`provenance_leaves`, asserting identical hash + Values (any bug in `descriptor_value`'s key mapping → hash mismatch → fail). **Plus `verification_leaves` → all 6 leaf families now verified cross-language.** A minimal `VerificationReport` (no evidence/guard/conflict sub-leaves) makes `verification_report_value` a total mapping; the report leaf's `evidence_digest = blake3(VERIFICATION_ROOT_DOMAIN ‖ canonical_verification_report_bytes)` re-derives in Python, and Rust (`verification_leaf_family_matches_cross_language_vector`) asserts the real engine `verification_leaves(Some(report))` matches. **C4-2 is complete**: JCS + blake3 Merkle + Ed25519 + `pack_root` + **all 6 leaf-family extractions** (budget/conflict/access/cell_set/provenance/verification, incl. `cell_content_hash` + report canonicalization) + the 6 Merkle roots of a real committed receipt — the receipt's entire canonicalization + crypto surface reproduces Rust↔Python byte-for-byte. task_8f2c7c22 dissolved. Gate `canonical-jcs-cross-language-check` (8 Rust tests + Python). | `canonical-jcs-cross-language-check` | Proved the receipt's two **core cryptographic canonicalization layers** are language-independent, both verified Rust↔Python by digest byte-match (collision infeasible → identical bytes → spec is normative). **(1) JCS canonical JSON** (`canonical_json_bytes`/`write_canonical_value`): pure-Python `json.dumps(sort_keys, separators=(',',':'), ensure_ascii=False)` over 8 vectors (sorted keys, escaping incl. `\"`/`\\`/`\n\r\b\f`, non-ASCII `café`, integers, nesting) — Rust `canonical::tests::jcs_cross_language_vectors_match`. **(2) blake3 Merkle roots** (`receipt.rs::merkle_root`): Python re-implements `blake3(domain‖0x00‖msg)` + the binary tree (leaf/node domains, odd-node duplication, empty-tree schema) over 5 vectors (0/1/2/3/4 leaves) — Rust `accountability::receipt_tests::merkle_root_matches_cross_language_vectors`. **Verify-only, golden-safe.** The Rust tests assert against the committed Python-derived fixtures, so the proof holds even where blake3 isn't `pip`-installed (the Python Merkle re-check skips gracefully). Gate `canonical-jcs-cross-language-check` (3 checks), in nightly + the F1.3 lane map (30 gates, audit green). | The 6 leaf-family field extractions + Ed25519 header verify + full-receipt-golden match remain, spawned task_8f2c7c22. | `canonical-jcs-cross-language-check` |
| C3-5 Receipt-schema minor-versioning procedure + ANN-degradation receipt-visibility review | Landed | **Both halves of C3-5 are now in place** (correcting the earlier "signed-golden-blocked" label). **(1) Additive-minor procedure:** [`RECEIPT_SCHEMA_VERSIONING.md`](RECEIPT_SCHEMA_VERSIONING.md) formalizes how a plan-visible option / signed-metadata field enters the canonical surface — schema-version bump + `schema_field_binding_v1.json` entry + determinism-golden re-baseline + the C4-2 cross-language run, all one PR — enforced by `canonical_field_sets_are_bound_to_schema_versions` (gate `canonical-schema-field-binding-check`: a field-set change without a fixture entry fails). Now **linked from the receipt spec** (`docs/spec/ACCOUNTABILITY_RECEIPT_V1.md` → "Changing the canonical (signed) field set"). **(2) A3.3 design-review record:** [`ADR-ann-degradation-receipt-visibility.md`](ADR-ann-degradation-receipt-visibility.md) records the Track A ↔ Track C decision — the per-collection ANN `serving_epoch` (+ serving mode) must enter the signed surface via the additive-minor procedure, derived from deterministic index state (rebuild count + transitions; no wall-clock/RNG), so a verifier re-executing the plan knows which serving mode to reproduce (else the "verifier re-executes plan" property silently breaks). Docs-only, no code/golden churn; binding gate green. | The A3.3 acceptance ref lands with A3.3's implementation; A7.3/A4.2 route their plan-visible options through the same procedure when they land. | `canonical-schema-field-binding-check` |
| C3-3 Receipt binding to agent handoff | Landed | Added optional `receipt_pack_root` + `receipt_signature_context` to `AgentHandoffRequest`/`AgentHandoffReport` (threaded through `plan_agent_handoff`), the B6.1 ledger encode/parse (hex-encoded, so the binding round-trips through the durable `0xc` cell), and the HTTP wire types + `/v1/handoff` handler (carried in + echoed back). Records **which** accountability receipt a handoff was made against so a consumer can re-verify it — **does not sign or alter any receipt**, so it's additive golden-safe (**correcting** the earlier "golden-rebaseline / signed bytes" label; body-encoded like B6.1, no new canonical/signed fields). Test proves the binding survives the ledger round-trip (`read_agent_handoff` == committed report); engine + api-types + server + SDK suites green; handoff-ledger + contract + OpenAPI-codegen gates green. | The optional `handoff_receipt_binding_check` gate + a cross-verify against a real signed receipt are follow-ons. | `handoff-ledger-check` |
| F08-B6.1 Durable handoff-ledger | Landed | `Database::commit_agent_handoff(source, target, request)` runs the same validation as `plan_agent_handoff`, then persists the report as a durable, auditable cell in a reserved `0xc` namespace (scope `__cortex_handoff__`, unreadable by any `AgentView`), returning `CommittedAgentHandoff { report, handoff_cell_id, committed_seq }`. `read_agent_handoff(cell_id)` parses it back for audit. Gated on the default-off `agent_transactions` flag → never in a golden; body-encoded (no new canonical metadata fields → golden-safe, correcting the earlier "needs regen tooling" classification). 3 tests: feature-flag, persist→read-back round-trip + **no retrieval leak** (0xc never a candidate), **cross-restart** audit read-back. fmt/clippy/api-freeze clean. Gate `handoff-ledger-check`. | Idempotent-by-key handoffs + the `/v1/handoff` HTTP surface (B6.3) build on it. | `handoff-ledger-check` |
| F04-B1.3 Persistent idempotency ledger | Landed | Replaced the echo-only idempotency in `commit_agent_transaction` with a durable ledger (`crates/cortex-engine/src/idempotency.rs`). A repeated `(agent_id, idempotency_key)` with the **same** blake3 request digest replays the prior `committed_seq` **without re-writing** (`idempotent_replay: true`); the same key with a **different** digest is rejected as reuse; a fresh key executes + records. Entries are durable cells in a reserved `0xb` namespace (alongside memory `0x8`/feedback `0x9`/session `0xa`) with a reserved scope no `AgentView` can read → never retrievable, never in a golden (default-off feature). Content-addressed cell-id from `(agent, key)` with **linear probing** on collision (each entry stores its full key, so two keys hashing to one slot are never aliased). 4 unit tests (deterministic digest, key round-trip with newline/`=` keys, forced-collision no-alias, slot range) + 5 end-to-end (replay-without-rewrite, reuse-rejected, distinct-keys-independent, **no retrieval leak**, **cross-restart persistence**). fmt/clippy/api-freeze clean. Gate `idempotency-ledger-check`. | HTTP/SDK `/v1/transactions` surface (B6.3) exposes it. | `idempotency-ledger-check` |
| F08-B6.2 Read-after-seq enforcement (engine primitive) | Landed | `Database::require_seq_visible(required_after_seq) -> EngineResult<CommitSeq>`: a `SharedSequenced` handoff consumer passes the handoff's `visible_after_seq` and gets the current seq back if visible, else a typed `EngineError::SequenceNotVisible { required, current }` — a hard failure instead of a silently-stale read. Purely additive (one new error variant, mapped into the existing 400/BadRequest bucket in `code()`); engine compiles, fmt/clippy/api-freeze clean; test proves visible→ok, future→typed-error, and that a commit advancing the seq flips it to ok. Gate `read-after-seq-check`. | The HTTP `min_seq` (→ hard 409) + AQL surface build on this primitive (B6.2-surface); B6.1 durable handoff-ledger is the sibling. | `read-after-seq-check` |
| F06-B4.5 Memory consolidation: retire (TTL-demote) — **F06 cluster complete** | Landed | `Database::retire_compression_sources(view, summary_cell_id, demote_ttl_seconds, now)`: after a summary is committed, shortens its **episodic** source cells' TTL so they expire `demote_ttl` from `now` (consolidated sources age out naturally) — never tombstones, never touches **semantic** sources, requires the summary readable + each demoted source writable. Read-modify-write of the existing `ttl_seconds`/`created_unix_seconds` header lines preserving body + all other metadata (**correcting** the earlier "golden-rebaseline / signed bytes" misclassification — it's a normal memory-cell mutation, golden-safe behind the default-off flag). 2 tests (feature-flag; and — using `seed_source_memories` where cell 1=decision/semantic, cell 2=observation/episodic — **only the episodic source is demoted, the semantic one skipped**, new expiry = now+demote, body intact). fmt/clippy/api-freeze clean. This closes the **F06 cluster**: classify (B4.1) → select (B4.2) → commit (B4.3) → unfold (B4.5-read) → retire (B4.5-write). Gate `memory-consolidation-check` (14 tests). | — | `memory-consolidation-check` |
| F06-B4.5 Memory consolidation: unfold (source resolve) | Landed | `Database::compression_sources(view, summary_cell_id) -> CompressionSourcesReport` — the `/v1/memory/cell/{id}/sources` engine core. Resolves a `semantic_summary`'s `compression_source_cells` **fail-closed**: the summary must be a readable semantic summary; any source in a scope the view cannot read is a hard `ScopeNotReadable` (never a silent drop); sources since removed are reported `missing_source_cell_ids` (not an error). Read-only, behind the default-off flag; golden-safe (reuses existing compression metadata). 4 integration tests (present/missing, non-summary reject, feature-flag, fail-closed on unreadable source). Gate `memory-consolidation-check`. | B4.5's retire half (TTL-demote episodic sources) + the HTTP/MCP surface (B4.4) are the follow-ons. | `memory-consolidation-check` |
| F06-B4.1/B4.2 Memory consolidation: classify + select | Landed | Engine-native, default-off, golden-safe. **B4.1** `memory_class(descriptor) -> Option<MemoryClass>` — a pure classifier (`decision`/`preference` = Semantic; `observation`/`workflow_result`/`error_log`/untyped session = Episodic; unknown subtype/non-memory = unclassified). **B4.2** `Database::semantic_compression_candidates(view, scope, now, freshness_below_q16, max_groups)` — the read-only selection half the existing `commit_semantic_memory_compression` consumes: deterministically picks readable episodic cells that have decayed below the freshness threshold (reusing the tested `memory_decay_scores` signal) and are **not** already a summary or already consolidated into one, grouped by subtype, stalest-first, capped at `max_groups`. Behind the `semantic_compression.enabled` flag (default false), so no default behaviour or goldens move; engine API-freeze + fmt + clippy clean. 3 unit + 4 integration tests. Gate `memory-consolidation-check`. | B4.3 conflict-guard is already in the commit path; next is B4.4 MCP consolidation worker + B4.5 unfold/retire API. | `memory-consolidation-check` |

| F5.2 Benchmark-score regression gate | Landed | `scripts/benchmark_floor_check.py` + committed `fixtures/benchmarks/floors.v1.json`: asserts each committed benchmark headline is at or above its recorded floor (ERB-500 combined ≥ 47.74, recall ≥ 55.71%), so a ranking/pipeline change that silently lowers a published score fails CI, not after release. Wired into the nightly `benchmark-validation` job. Gate `benchmark-floor-check`. | Add per-type floors + a history trend as more runs commit. | `benchmark-floor-check` |

| C4-1 Receipt-emission p99 budget | Landed | `receipt_emission_p99_is_within_budget` (`accountability/receipt_sign_tests.rs`): measures the receipt emission hot path (canonicalize + Ed25519-sign the header) over 400 iterations and asserts p99 stays under a frozen budget (50 ms/emit). Release-build measured **p50 ≈ 146 µs, p99 ≈ 155 µs** — a 325× margin, so it never flakes on CI noise yet catches a catastrophic emission regression (which would tempt operators to flag the receipt off → moat gone). Writes a report for tracking. Gate `receipt-emission-budget-check`. | Add a per-machine history/trend as release runs accumulate. | `receipt-emission-budget-check` |

| C4-3 Live anti-absorption proof in nightly | Landed | Wired the moat-proof gates into the nightly `benchmark-validation` job: `aab-conformance-check` (the thin Postgres+pgvector+OPA reference demonstrably fails ≥3 axes — equivocation, plan-bound access, dropped-conflict), `gce-spec-doc-check`, `receipt-threat-model-check`, and `receipt-emission-budget-check` (C4-1). All pass locally; YAML validated. So the category contract + its anti-absorption evidence run every night. | Live 4-competitor matrix (F4.3) needs a docker operator machine — out of critical path per the plan. | nightly `benchmark-validation` |

## Cross-cutting landings

| Item | State | Notes | Gate |
| --- | --- | --- | --- |
| B1.1 Feedback silent-overwrite | Landed | `next_feedback_cell_id` probes for a free id in a loop (mirrors session-id allocation) so a second feedback write cannot clobber the first. | engine tests |
| TE1 `search` MCP tool | Landed | Permission-scoped `search` tool across `tools.rs`/`server.rs`/`sdk_executor.rs` + `MCP.md`. Now supports `mode=semantic\|hybrid\|auto`: the agent sends text only and the server embeds the query (`embed_query=true`) — semantic search reaches the MCP surface. SDK gains `search_embedded` (sync + async). | mcp tests |
| Track F baseline | Landed | Per-PR `retrieval-recall-baseline-check` with cross-mode adversarial fixtures; foundation for detecting silent recall regressions. | `retrieval-recall-baseline-check` |

## Reading this ledger

The distinction that matters here is *slice* vs *full task*. The ingest, query,
and corpus-backfill embedding paths are usable end-to-end today, and the store
now records embedding-profile provenance and fails closed on a model/dim/metric
mismatch (A2.1). What remains is promoting `embedding_ref` into the canonical
accountability receipt (a C3-5 minor schema bump) and A2.2's checkpoint/HNSW
interaction proofs. Those are tracked in the *Not yet* columns above.

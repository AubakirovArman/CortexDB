[package]
name = "cortex-receipt-verify"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
cortex-crypto = { path = "../cortex-crypto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[package]
name = "cortex-embed-client"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
cortex-engine = { path = "../cortex-engine" }
serde_json = "1"
ureq = { version = "2", features = ["tls"] }
